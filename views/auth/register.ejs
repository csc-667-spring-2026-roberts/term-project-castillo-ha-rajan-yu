<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | Gator Tycoons</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <header>
      <nav class="nav">
        <div class="brand">Gator Tycoons</div>
        <div class="nav-actions">
          <% if (typeof user !== "undefined" && user) { %>
          <form action="/auth/logout" method="post">
            <button type="submit">Logout</button>
          </form>
          <% } else { %>
          <a href="/auth/login">Login</a>
          <a href="/auth/register">Register</a>
          <% } %>
        </div>
      </nav>
    </header>
    <main>
      <%# M7: Registration page with POST form and inline error display. %>
      <h1>Create Your Account</h1>
      <p>Register to continue to your game lobby.</p>

      <%# Server-side validation and conflict errors are rendered here. %>
      <% if (error) { %>
      <div class="error"><%= error %></div>
      <% } %>

      <%# Form posts to /auth/register and uses PRG redirect on success. %>
      <form method="post" action="/auth/register">
        <label for="display_name">Display Name</label>
        <input
          id="display_name"
          name="display_name"
          type="text"
          required
          maxlength="50"
          value="<%= formData?.display_name ?? "" %>"
        />

        <label for="email">Email</label>
        <input
          id="email"
          name="email"
          type="email"
          required
          maxlength="255"
          value="<%= formData?.email ?? "" %>"
        />

        <label for="password">Password</label>
        <input id="password" name="password" type="password" required minlength="8" />

        <button class="button" type="submit">Register</button>
      </form>

      <div class="meta-row">
        <span>Already have an account?</span>
        <a href="/auth/login">Go to Login</a>
      </div>
    </main>
  </body>
</html>
